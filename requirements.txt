# GP Triage POC - Dependencies
# Compatible with Python 3.12+ and Python 3.13+

# Core framework
streamlit>=1.29.0,<2.0

# LangChain ecosystem - 0.2.x series (pydantic v2 native)
langchain>=0.2.0,<0.3
langchain-openai>=0.1.8,<0.2
langchain-community>=0.2.0,<0.3
langchain-core>=0.2.0,<0.3
langchain-chroma>=0.1.0,<0.2

# OpenAI
openai>=1.14.0,<2.0

# Vector database
# Pinned to 0.4.22 + compatible protobuf/grpc to avoid the
# google.protobuf _CheckCalledFromGeneratedFile() crash on Streamlit Cloud.
chromadb==0.4.22
protobuf==3.20.3
grpcio==1.59.3
opentelemetry-api==1.20.0
opentelemetry-sdk==1.20.0
opentelemetry-exporter-otlp-proto-grpc==1.20.0

# Utilities
python-dotenv==1.0.0
pandas>=2.2.0,<4.0       # 2.2+ has Python 3.13 wheels; 2.1.4 does not
numpy>=1.26.0,<2.0.0     # 1.26+ has Python 3.13 wheels

# Pydantic v2 (>=2.13 has Python 3.13 cp313 wheels)
pydantic>=2.13.0
pydantic-core>=2.46.0

# HTTP client (explicit injection to avoid httpx proxies= crash)
httpx>=0.27.0,<1.0

# PostgreSQL / Supabase
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0,<3.0
supabase>=2.0.0

# Document generation
python-docx>=1.1.0

# Development/Testing
pytest>=7.4.0
black>=23.12.0
